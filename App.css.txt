/* General Styles */
body {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  background-color: #121212; /* Dark Mode Background */
  font-family: 'Poppins', sans-serif;
}

.container {
  max-width: 850px;
  margin: 20px auto;
  padding: 40px;
  background: #1e1e1e; /* Darker Background */
  border-radius: 12px;
  box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
  text-align: center;
}

h1 {
  color: #00adb5; /* Neon Cyan Title */
  font-size: 28px;
  font-weight: bold;
  text-transform: uppercase;
}

/* Form Styles */
.form-group {
  margin-bottom: 15px;
  text-align: left;
}

.form-group label {
  display: block;
  font-weight: bold;
  color: #ffffff; /* White Labels */
}

.form-group input {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: 1px solid #393e46;
  background: #222831;
  color: white;
  outline: none;
}

.form-group input::placeholder {
  color: #888;
}

/* Buttons */
button {
  width: 100%;
  padding: 12px;
  margin-top: 12px;
  border-radius: 8px;
  border: none;
  font-size: 16px;
  cursor: pointer;
  font-weight: bold;
  transition: 0.3s;
}

button:hover {
  opacity: 0.8;
}

/* Specific Button Colors */
.btn-submit {
  background-color: #00adb5; /* Neon Cyan */
  color: white;
}

.btn-edit {
  background-color: #ff9800; /* Orange */
  color: white;
}

.btn-delete {
  background-color: #ff3d00; /* Red */
  color: white;
}

.btn-checkout {
  background-color: #4caf50; /* Green */
  color: white;
}

/* Seats Info */
.seats {
  font-size: 20px;
  font-weight: bold;
  color: #00adb5;
  margin-top: 20px;
}

/* Table Styles */
table {
  width: 100%;
  margin-top: 20px;
  border-collapse: collapse;
  background: #222831;
  border-radius: 8px;
}

th, td {
  padding: 12px;
  text-align: center;
  border: 1px solid #393e46;
  color: white;
}

th {
  background: #00adb5; /* Neon Cyan Header */
  color: black;
}

td button {
  margin: 5px;
  padding: 8px 12px;
  font-size: 14px;
}
